{
	"name": "actix_svelte_template",
	"version": "0.0.1",
	"scripts": {
		"dev": "npm-run-all --parallel dev:actix dev:svelte",
		"dev:svelte": "cd client && npm run dev",
		"dev:actix": "cd src && cargo watch -x run --why"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
